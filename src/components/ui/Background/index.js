import styled from "styled-components/macro";

const Background = styled.div`
  width: 100vw;
  height: 100vh;
  margin: 0 auto;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  background-image: linear-gradient(-30deg, #ffffff -100%, #ffd100);
`;

export default Background;
